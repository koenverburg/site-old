import * as React from 'react'
import { Section, Props } from '../index'

export default {
  title: 'Skeleton/Section',
  component: Section,
}

const Template = (args: Props) => (<Section />)

const Default = Template.bind({})
// @ts-ignore
Default.args = {}
